name: classic_logger
description: It is a cross-platform classic and minimalist logger written in the Dart language.
version: 1.0.0
repository: https://github.com/myConsciousness/classic-logger.dart

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  intl: ^0.18.1

dev_dependencies:
  lints: ^2.0.1
  test: ^1.24.2
  import_sorter: ^4.6.0
